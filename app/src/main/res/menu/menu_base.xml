<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/action_main"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Main Activity"/>
        <item
            android:id="@+id/action_other"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Other Activity"/>

        <item
            android:id="@+id/lista_nominas"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Lista"/>

        <item
            android:id="@+id/lista_sql"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Lista Web Service"/>

        <item
            android:id="@+id/action_noHamburger"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="No Hamburger"/>
    </group>
    <item
        android:id="@+id/navigation_subheader"
        android:title="Otras opciones">
        <menu>
            <item
                android:id="@+id/action_noToolbar"
                android:icon="@android:drawable/ic_btn_speak_now"
                android:title="No Toolbar"/>
            <item
                android:id="@+id/ic_share"
                android:icon="@android:drawable/ic_btn_speak_now"
                android:title="Opcion 2"/>
        </menu>
    </item>

    <item
        android:icon="@mipmap/ic_settings"
        android:title="Ajustes"/>
</menu>
