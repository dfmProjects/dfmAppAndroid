<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorDfm</item>
        <item name="colorAccent">@color/colorDfm</item>
    </style>

    <style name="AppTheme.MyDrawerStyle">
        <!-- Estilo para el menu lateral. -->
        <item name="android:textSize">16dp</item>

    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MainActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorDfm</item>
        <item name="colorPrimaryDark">@color/colorDfm</item>
        <item name="colorControlActivated">@color/colorDfm</item>
        <item name="android:textColorPrimary">@color/colorWhite</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>
</resources>
